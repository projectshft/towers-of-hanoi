var board = [
  {peg: 1, disk: ["5", "4", "3", "2", "1"]},
  {peg: 2, disk: []},
  {peg: 3, disk: []}
];

var startingBoard = board.map (function (game) {
  return game;
});

var moveDisk = startingBoard.map (function (move) { 
  var newPostion = [];
  if(this.peg == 1 && this.disk.includes(this.disk.length(-1))) {
    newPosition.push();
   
   
 } else {
   
 }
});

console.log(startingBoard);

















































